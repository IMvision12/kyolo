# ---------------------------------------------------------------------------
# kyolo runtime requirements
# ---------------------------------------------------------------------------
# Keras 3 is backend-agnostic. Install exactly ONE backend below (TensorFlow,
# JAX or PyTorch). The library itself is written entirely in `keras.ops`, so it
# runs unchanged on any of them.
# ---------------------------------------------------------------------------

# Core
keras>=3.5.0
numpy>=1.24

# --- Choose a backend (uncomment one) --------------------------------------
tensorflow>=2.16
# jax[cpu]>=0.4.28
# torch>=2.2 ; extra-index for CPU wheels: --extra-index-url https://download.pytorch.org/whl/cpu

# --- Weight conversion (optional) ------------------------------------------
# Needed only to run the per-model convert_*.py / `kyolo-convert` against checkpoints.
# The AGPL-3.0 weights are NOT distributed with this project.
torch>=2.2
torchvision>=0.17
ultralytics>=8.2.0

# --- Visualization (optional) ----------------------------------------------
matplotlib>=3.7
pillow>=10.0
tqdm>=4.66
